<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>taskId</includeMdcKeyName>
            <includeMdcKeyName>taskType</includeMdcKeyName>
            <includeMdcKeyName>workerId</includeMdcKeyName>
            <includeMdcKeyName>status</includeMdcKeyName>
            <customFields>{"service":"worker-service"}</customFields>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Reduce noise from framework classes -->
    <logger name="org.apache.kafka" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.springframework" level="WARN"/>
    <logger name="com.orchestrator" level="INFO"/>
</configuration>
